/*
  动态SQL路由注册，实际使用中可能涉及到使用中间件，比如权限验证等，需要加到具体项目中。
*/
package directsql

/*
import (
	"github.com/lessgo/lessgo"
	"lessdo/middleware"
)

//注册路由
func init() {
	lessgo.Root(
		// bos 执行SQL定义的路由
		lessgo.Leaf("/bos/*path", DirectSQL),
		lessgo.Leaf("/bom/reloadall", DirectSQLReloadAll),
		lessgo.Leaf("/bom/reload/*path", DirectSQLReloadModel),
	)
	return
}
*/
